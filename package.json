{
  "name": "bacnet",
  "version": "0.0.1",
  "scripts": {
    "configure": "node-gyp configure && npm run build",
    "clean": "node-gyp clean",
    "rebuild": "node-gyp rebuild && npm test",
    "build": "node-gyp build && npm test",
    "test": "standard && mocha spec",
    "style": "standard"
  },
  "devDependencies": {
    "mocha": "^2.3.4",
    "should": "^8.0.1"
  },
  "dependencies": {
    "nan": "^2.1.0"
  },
  "standard": {
    "ignore": ["bacnet-stack"]
  }
}
