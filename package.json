{
  "name": "bacnet",
  "version": "0.0.1",
  "scripts": {
    "configure": "node-gyp configure && npm run build",
    "build": "node-gyp build && npm test",
    "test": "node test/test.js"
  },
  "devDependencies": {
  },
  "dependencies": {
    "nan": "^2.1.0"
  }
}
