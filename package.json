{
  "name": "bacnet",
  "version": "0.0.1",
  "scripts": {
    "configure": "node-gyp configure && npm run build",
    "build": "node-gyp build && npm test",
    "test": "mocha spec"
  },
  "devDependencies": {
    "mocha": "^2.3.4",
    "should": "^8.0.1"
  },
  "dependencies": {
    "nan": "^2.1.0"
  }
}
